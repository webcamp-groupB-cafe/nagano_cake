<div class="container">
  <div class="row">
    <%= flash[:notice] %>
    <div class="col-md-3"></div>
    <div class="col-md-9">
      <div class="top-box">
        <div class="top-box2">
          <p>ようこそ、ながのCAKEへ！<br>
          このサイトは、ケーキ販売のECサイトになります。<br>
          会員でない方も商品の閲覧はできますが、<br>
          購入には会員登録が必要になります。
          </p>
        </div>
      </div>

          <h2>新着商品</h2>
        <div class="new_items" style="display: flex; justify-content: space-around; ">
          <% @items.each do |item| %>
          <% if item.image.attached? %>
          <%= image_tag item.image, size: "100x100" %>
          <% else %>
          <%= image_tag 'no_cake', size: "100x100" %>
          <% end %>
          <p><%= item.name %></p>
           <p>￥<%= item.with_tax_price.to_i %></p>
          <% end %>
          </div>
          <p style="text-align: right;"><%= link_to "全ての商品を見る >", items_path %></p>
    </div>
  </div>
</div>

